# generate_spin_visualizations  
运行之后会扫描当前文件夹下的所有文件夹，然后在对应文件夹内生成对应的图片  
# classify_png_images  
放到运行完test.py的有序或者无序文件夹下，运行    
会自动创建一个文件夹，里面是分类后的图片  
# xy_model_PhaseData_analysis  
放到运行完test.py的相变文件夹下，运行  
会自动创建classify文件夹，里面是对应的温度点文件夹  
然后就是各个温度点的图片  

