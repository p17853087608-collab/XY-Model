## 第一部分：正确的工作流程（数据处理顺序）

### 1. 原始数据准备与初步计算
**输入**：5个系统尺寸（L=16, 32, 64, 128, 256），每个尺寸有60个温度点，每个温度点有150个CNN预测概率值。
**操作**：
- 对每个尺寸L的每个温度点，计算150个概率值的平均值 → 得到平均概率曲线 `P_mean(T, L)`
- 对每个温度点，计算150个概率值的标准差 → 得到误差估计 `P_std(T, L)`
**输出**：5条平均概率曲线及其误差（用于图2a）

### 2. 确定有限尺寸相变温度 T_c(L)
**操作**：
- 对每个尺寸L的平均概率曲线，找到概率值最接近0.5的温度点
- 该温度点即为该尺寸的相变温度 `T_c(L)`（原始点估计）
**输出**：5个 `T_c(L)` 的原始点估计值

### 3. 单尺寸Bootstrap分析（关键步骤）
**操作**：
- 对每个尺寸L，进行1000次Bootstrap重采样：
  1. 对每个温度点：从150个原始概率值中有放回地抽取150个值
  2. 计算新样本的平均概率曲线
  3. 从新曲线上找到最接近0.5的温度 → 得到一个 `T_c(L)` 的重采样估计
- 对每个尺寸L，得到1000个 `T_c(L)` 的重采样估计值
**输出**：
- 每个尺寸L的1000个 `T_c(L)` 估计值（用于图5箱线图）
- 每个尺寸L的 `T_c(L)` 均值（用于图3数据点）
- 每个尺寸L的 `T_c(L)` 标准差（用于图3误差棒）

### 4. BKT标度拟合与外推
**操作**：
- 计算每个尺寸L的 `x = 1/(ln L)²`
- 以 `x` 为横坐标，步骤3得到的 `T_c(L)` 均值为纵坐标，进行加权线性拟合：
  `T_c(L) = T_BKT + A × x`
- 权重为每个点标准差的倒数
**输出**：
- 拟合参数 `T_BKT` 和 `A`
- 拟合直线方程（用于图3）

### 5. 全流程Bootstrap（评估T_BKT不确定性）
**操作**：
- 进行1000次完整流程的Bootstrap：
  1. 对每个尺寸L：执行步骤3的单次Bootstrap，得到一个 `T_c(L)` 值
  2. 用5个尺寸的 `T_c(L)` 值拟合BKT公式，得到一个 `T_BKT` 估计值
- 收集1000个 `T_BKT` 估计值
**输出**：1000个 `T_BKT` 的Bootstrap估计值（用于图4直方图）

### 6. 数据变换与对齐
**操作**：
- 使用步骤4得到的 `T_BKT` 和步骤2得到的原始 `T_c(L)` 点估计
- 对每个尺寸L的概率曲线进行平移变换：
  `T' = T - [T_c(L) - T_BKT]`
- 概率值 `P_order` 保持不变，误差棒也保持不变
**输出**：5条变换后的概率曲线（用于图2b）

---

## 第二部分：需要绘制的图表及其思路与作用

| 图号 | 图表内容 | 绘制思路 | 作用与目的 |
|------|----------|----------|------------|
| **图1** | CNN方法流程图 | 使用框图展示完整流程：蒙特卡洛模拟→自旋构型图像→CNN分类器训练（极端温度策略）→概率预测→曲线分析→BKT外推 | 直观展示研究方法，帮助读者快速理解从物理系统到机器学习再到物理结论的完整链路 |
| **图2a** | 原始概率曲线（带误差棒） | 在同一坐标系中绘制5条曲线：X轴为温度T，Y轴为平均概率P_order，每条曲线代表一个尺寸L。添加Y=0.5参考线，标记每条曲线与参考线的交点（T_c(L)）。每个数据点带有垂直误差棒（标准差）。 | 展示原始数据质量，显示CNN预测的稳定性，直观呈现有限尺寸效应（不同尺寸曲线交点位置不同） |
| **图2b** | 标度变换后的概率曲线（带误差棒） | 使用图2a相同的数据，但X轴变换为T' = T - [T_c(L) - T_BKT]。Y轴和误差棒不变。添加Y=0.5水平参考线和X=T_BKT垂直参考线。 | 验证BKT标度变换的正确性：所有曲线应在T_BKT处相交于P_order=0.5，证明不同尺寸系统在标度变换下具有相同的相变行为 |
| **图3** | BKT标度拟合图 | 以1/(ln L)²为横坐标，T_c(L)均值为纵坐标，绘制5个数据点（带误差棒）。用加权最小二乘法拟合直线，在图中显示拟合公式和T_BKT值。在横坐标零点处标注T_BKT。 | 定量验证BKT标度律，通过线性外推得到热力学极限下的相变温度T_BKT，这是论文的核心定量结果 |
| **图4** | T_BKT的Bootstrap分布 | 将1000个T_BKT估计值绘制为直方图。添加三条参考线：均值（红色虚线）、95%置信区间上下界（橙色虚线）。在图中标注均值和置信区间数值。 | 量化最终结果T_BKT的统计不确定性，证明结果的稳健性和可靠性，展示误差范围 |
| **图5** | 有限尺寸效应箱线图 | 对每个尺寸L，用其1000个T_c(L)估计值绘制一个箱线图（共5个箱子）。用线段连接各箱体中位数，添加T_BKT水平参考线。X轴为系统尺寸L，Y轴为T_c(L)。 | 直观展示T_c(L)的统计分布随系统尺寸的变化规律，显示收敛趋势和不确定性变化，补充图3的点估计信息 |

---

## 关键要点总结

1. **数据依赖关系**：
   - 图2a → 图3（提供T_c(L)点估计）
   - 步骤3的Bootstrap结果 → 图3（提供误差棒）和图5（提供箱线图数据）
   - 图3结果（T_BKT）→ 图2b（提供平移量）
   - 步骤5的全流程Bootstrap → 图4

2. **图表逻辑关系**：
   - 图1：展示方法（如何做）
   - 图2：展示原始数据和变换后数据（看到了什么）
   - 图3：展示理论验证和核心结果（证明了什么）
   - 图4：展示结果可靠性（结果有多可信）
   - 图5：补充展示统计分布（更全面的视图）

3. **时间安排建议**：
   - **第1周**：完成步骤1-2，绘制图2a
   - **第2周**：完成步骤3，得到图3和图5的数据基础
   - **第3周**：完成步骤4-5，绘制图3、图4、图5
   - **第4周**：完成步骤6，绘制图2b和图1，整合所有图表

这个工作流程确保每个步骤都有明确输入输出，每张图表都有可靠的数据来源，整个分析过程科学严谨。